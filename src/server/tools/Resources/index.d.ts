import { Tool } from '../Tool';
import * as Express from 'express';
export declare class Resources extends Tool {
    /**
     * Boot the resources tool
     *
     * @return {void}
     *
     */
    boot(app: Express.Application): void;
}
